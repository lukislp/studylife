@page "/connect/mcp"
@layout PublicLayout

@* Thin wrapper: the actual consent UI/logic lives in Components/ConnectConsent.razor, shared with
   /connect/capture (see ConnectCapture.razor) - see that component for the full flow writeup
   (identity contract v1 §2). This page is the "mcp" audience: studylife-mcp's OAuth authorize
   step redirects the browser here with redirect_uri/state (either a normal https callback, or -
   for the stdio `mcp --login` consumer with no https origin of its own - an RFC 8252 loopback
   http://127.0.0.1|localhost redirect_uri). *@

<ConnectConsent Audience="mcp" />
